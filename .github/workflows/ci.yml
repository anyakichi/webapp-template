name: lint
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    container: anyakichi/webapp-template-builder
    steps:
      - uses: actions/checkout@v2
      - run: yarn install
      - run: yarn lint
